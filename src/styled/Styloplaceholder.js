/* import { makeStyles } from "@mui/styles";
import { Styloplaceholder } from 'module';

export const Styloplaceholder = makeStyles((theme) => ({
	miPlaceholder: {
		"&::placeholder": {
			color: "gray", // Establece el color del placeholder en gris
			fontStyle: "italic", // Establece el estilo del texto del placeholder en cursiva
		},
	},
})); */
